import styled from 'astroturf';

const Flex = styled.div`
  display: flex;

  &.aic {
    align-items: center;
  }

  &.posr {
    position: relative;
  }

  &.jcsb {
    justify-content: space-between;
  }
`;

export default Flex;
